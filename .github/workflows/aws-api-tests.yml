name: AWS API tests

on:
  pull_request:
    paths:
      - 'painel-obrigacoes/aws/api/**'
      - 'painel-obrigacoes/js/**'
      - 'painel-obrigacoes/css/**'
      - 'painel-obrigacoes/aws/template.yaml'
      - 'modules/obrigacoes/contracts/**'
      - 'modules/contracts/**'
      - 'modules/registry.js'
      - 'platform/**'
      - 'index.html'
      - 'test/**'
      - '.github/workflows/aws-api-tests.yml'
  workflow_dispatch:

permissions:
  contents: read

jobs:
  test:
    runs-on: ubuntu-latest
    defaults:
      run:
        working-directory: painel-obrigacoes/aws/api
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Use Node.js 22
        uses: actions/setup-node@v4
        with:
          node-version: '22'

      - name: Install dependencies
        run: npm install --no-audit --no-fund

      - name: Run API test suite
        run: npm test

      - name: Run platform registry tests
        run: npm test
        working-directory: .

      - name: Check platform shell syntax
        run: node --check platform/shell.js
        working-directory: .

      - name: Build Lambda bundles
        run: npm run build
